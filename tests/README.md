# Regression cases

These .yaml files use JSON syntax, a valid YAML 1.2 subset. Each case has id, user, context, expected_action, must and must_not.

Fixtures define the available evidence; they do not claim live retrieval. Evaluate decisions and semantic behavior, not exact wording. Follow the manual protocol in ../CONTRIBUTING.md. No automated model evaluation has been run by creating these files.
